name: Build artifacts

on:
  push:
    branches:
      - 'main'

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Build artifacts
        run: bash ${GITHUB_WORKSPACE}/run.sh
